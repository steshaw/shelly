# vim: set filetype=bash:
# shellcheck shell=bash

nvim_default() {
  light_theme='zellner'

  if is_dark_mode; then
    nvim "$@"
  else
    command nvim +"colorscheme $light_theme" "$@"
  fi
}

alias nvim=nvim_default
