sourceExists ~/.nix-profile/etc/profile.d/nix.sh

if [[ "$(uname)" == 'Darwin' ]]; then
  nixpkgs=~/Projects/steshaw/nixpkgs
  if [[ -d ${nixpkgs} ]]; then
    export NIX_PATH="nixpkgs=${nixpkgs}"
  fi
fi
