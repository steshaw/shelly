#!/usr/bin/env bash

set -euo pipefail

if [ -x "$(which gnome-open 2>&-)" ]; then
  command=gnome-open
elif [ -x "$(which xdg-open 2>&-)" ]; then
  command=xdg-open
elif [ -x "$(which open 2>&-)" ]; then
  command=open
else
  alias o=echo
fi

$command "$@"
