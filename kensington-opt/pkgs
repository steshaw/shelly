#!/usr/bin/env bash

set -euo pipefail

usage() {
  echo "usage $0: install | delete pkg"
  exit 2
}

pkgs=(
  doom-emacs
  go-1.18beta1
)

install_dir=../local
mkdir -p $install_dir

if [[ $# -eq 1 && $1 == install ]]; then
  stow --target $install_dir --stow "${pkgs[@]}"
elif [[ $# -eq 2 && $1 == delete ]]; then
  stow --target $install_dir --delete "$2"
else
  usage
fi
