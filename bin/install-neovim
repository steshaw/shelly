#!/usr/bin/env bash

set -euo pipefail

osType=$(uname)

case $osType in
  Linux|Darwin)
    brew install neovim python
    pip3 install --upgrade pip
    pip3 install neovim
    ;;
  *)
    echo "Unsupported OS $osType"
    exit 1
esac
