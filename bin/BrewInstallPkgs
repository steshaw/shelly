#!/usr/bin/env bash

set -eu

brew tap homebrew/versions

shellyEtc=$SHELLY_HOME/etc

packages=$(
  cat ${shellyEtc}/brew.pkgs |
  grep -v '^#' |
  grep -v '^\s*$' |
  grep -v --fixed-strings --file ${shellyEtc}/brew.pkgs.exceptions
)

for pkg in ${packages}; do
  brew install "${pkg}"
done
