#!/usr/bin/env bash

set -eu

title="${USER}@$(hostname) (added by shelly/bootstrap)"
key="$(cat ~/.ssh/id_rsa.pub)"
user="${USER}"

echo "Authenticating to GitHub as ${user}"

curl -u "${user}" -d "{\"title\": \"${title}\", \"key\": \"${key}\"" https://api.github.com/user/keys
