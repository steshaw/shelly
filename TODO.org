- Auto install ☕ Brew? i.e. Homebrew/Linuxbrew.
  - Or use the venerable [[https://nixos.org/nix/][Nix]]?

- Bootstrap vim with `bin/vim-plug-install`?

- Install bash-completion package?

- Seems needed for intero/stack:
  - sudo apt install libtinfo-dev

- Install "common" dev tooling?
  - sudo apt -y install build-essential
  - sudo apt -y install libssl-dev
